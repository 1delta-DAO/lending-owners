name: Fetch MOONWELL owners

on:
  schedule:
    - cron: "0 3,7,11,15,19,23 * * *"
  workflow_dispatch:

concurrency:
  group: fetch-moonwell
  cancel-in-progress: true

permissions:
  contents: write

jobs:
  fetch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 10

      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - name: Create .env
        run: echo "MOONWELL_SUBGRAPH_API_KEY=${{ secrets.MOONWELL_SUBGRAPH_API_KEY }}" > .env

      - name: Fetch owners
        run: pnpm --filter @lending-owners/runner run start -- --lender MOONWELL

      - name: Commit result
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/MOONWELL.json
          git diff --staged --quiet && echo "no changes" || (
            git commit -m "updating MOONWELL owners - $(date -u '+%Y-%m-%dT%H:%M:%SZ')"
            git pull --rebase origin main
            git push
          )
